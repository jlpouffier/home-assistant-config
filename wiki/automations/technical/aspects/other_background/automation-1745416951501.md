# Changer face du cube / Changer face du cube

## English
- Back to guest-friendly view: [other_background](../../../aspects/other_background.md)
- Back to technical aspect index: [other_background](../other_background.md)

### Summary
- Runs when: A trigger fires; A trigger fires; A trigger fires; A trigger fires; A trigger fires; A trigger fires; A trigger fires; A trigger fires; A trigger fires; A trigger fires; A trigger fires; A trigger fires
- Only if: No extra conditions
- Then: Execute the flow below

### Flow (trace-style)
- Triggers:
  - A trigger fires
  - A trigger fires
  - A trigger fires
  - A trigger fires
  - A trigger fires
  - A trigger fires
  - A trigger fires
  - A trigger fires
  - A trigger fires
  - A trigger fires
  - A trigger fires
  - A trigger fires
- Conditions (all must be true):
  - No extra conditions
- Actions:
  - CHOOSE (first match)
    - Branch 1
      - Check: Trigger “eating” fired
      - THEN
        - Set Face du cube (Bureau) to “Pause repas”
    - Branch 2
      - Check: Trigger “away” fired
      - THEN
        - Set Face du cube (Bureau) to “Absent”
    - Branch 3
      - Check: Trigger “dnd” fired
      - THEN
        - Set Face du cube (Bureau) to “Ne pas déranger”
    - Branch 4
      - Check: Trigger “meeting” fired
      - THEN
        - Set Face du cube (Bureau) to “Réunion”
    - Branch 5
      - Check: Trigger “available” fired
      - THEN
        - Set Face du cube (Bureau) to “Disponible”
    - Branch 6
      - Check: Trigger “coffee” fired
      - THEN
        - Set Face du cube (Bureau) to “Pause café”

## Français
- Retour vers la vue “invité” : [other_background](../../../aspects/other_background.md)
- Retour vers l’index technique de l’aspect : [other_background](../other_background.md)

### Résumé
- Se déclenche quand : Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit; Un déclencheur se produit
- Uniquement si : Pas de condition supplémentaire
- Ensuite : Exécuter le déroulé ci-dessous

### Déroulé (style ‘trace’)
- Déclencheurs :
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
  - Un déclencheur se produit
- Conditions (toutes doivent être vraies) :
  - Pas de condition supplémentaire
- Actions :
  - CHOISIR (première branche valide)
    - Branche 1
      - Vérifier : Le déclencheur “eating” s’est produit
      - ALORS
        - Mettre Face du cube (Bureau) sur “Pause repas”
    - Branche 2
      - Vérifier : Le déclencheur “away” s’est produit
      - ALORS
        - Mettre Face du cube (Bureau) sur “Absent”
    - Branche 3
      - Vérifier : Le déclencheur “dnd” s’est produit
      - ALORS
        - Mettre Face du cube (Bureau) sur “Ne pas déranger”
    - Branche 4
      - Vérifier : Le déclencheur “meeting” s’est produit
      - ALORS
        - Mettre Face du cube (Bureau) sur “Réunion”
    - Branche 5
      - Vérifier : Le déclencheur “available” s’est produit
      - ALORS
        - Mettre Face du cube (Bureau) sur “Disponible”
    - Branche 6
      - Vérifier : Le déclencheur “coffee” s’est produit
      - ALORS
        - Mettre Face du cube (Bureau) sur “Pause café”
